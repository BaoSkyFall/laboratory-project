

export class ExportHelper {

    // public static getListByPage(list: any[],
    //     page: { pageSize: number | string, pageNumber: number | string } | undefined = undefined) {
    //         if (!page) return list

    //         const pageNumber = page.pageNumber ? page.pageNumber : 1;
    //         const listbyPage = list.map((item, idx) => {

    //         });
    // };
};